name: 05 - Event Filters
on: 
    push:
        branches: #main [main, otra]
            - main

jobs:
  echo:
    runs-on: ubuntu-latest
    steps: 
      - run: echo 'Running whenever branch is main'